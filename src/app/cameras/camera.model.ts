export class Camera {
  public CODE: string;
  public NAME: string;
  public URL: string;

  constructor(code: string, name: string, url: string) {
    this.CODE = code;
    this.NAME = name;
    this.URL = url;
  }
}
